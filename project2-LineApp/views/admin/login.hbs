<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">

            <h1>Inicio </h1>

            <form action="/login" method="POST">
                <div class='form-group'>
                    <label for='username'>Credenciales</label>
                    <input name='username' type='text' class='form-control' placeholder='Credenciales'
                        id='username'>
                </div>
                <div class='form-group'>
                    <label for='password'>Contraseña</label>
                    <input name='password' type='password' class='form-control' placeholder='******' id='password'>
                </div>
                <button type='submit' class='btn btn-dark btn-block'>Iniciar sesión</button>
                {{#if errorMsg}}
                <p style="margin-top: 10px;background: red; color: white; text-align: center; padding: 10px">
                    {{errorMsg}}</p>
                {{/if}}
            </form>

        </div>
    </div>
</div>
